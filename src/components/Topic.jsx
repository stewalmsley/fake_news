import React from 'react';
import PropTypes from 'prop-types';

const Topic = props => {
    return (
        <div>
            
        </div>
    );
};

Topic.propTypes = {
    
};

export default Topic;