import React from 'react';
import PropTypes from 'prop-types';

const Sidebar = props => {
    return (
        <div>
            <h2>Test</h2>
        </div>
    );
};

Sidebar.propTypes = {
    
};

export default Sidebar;